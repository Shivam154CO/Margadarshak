# CollegeMap Fixes TODO

## Tasks:
1. [ ] Restyle CollegeMap.tsx to match Dashboard/CollegeSearch design (clean, professional UI)
2. [ ] Keep all existing logic intact (data fetching, map features, filters, etc.)
3. [ ] Remove emoji from Profile.tsx (✅, 🎓, ❌)
4. [ ] Remove emoji from ProfileView.tsx (✅)
5. [ ] Test the changes

## Design Changes for CollegeMap:
- Use `text-slate-900` instead of `text-gray-900`
- Use `font-bold` instead of `font-black` for most text
- Use `bg-white`, `border-slate-200` for cards
- Remove complex gradient backgrounds and decorative blur elements
- Simplify animations (keep subtle transitions, remove pulsing/overly dramatic effects)
- Use consistent card styling with `rounded-2xl` or `rounded-xl`
- Match the clean, professional look of Dashboard and CollegeSearch

